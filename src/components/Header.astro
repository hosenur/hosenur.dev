---
const currentPath = Astro.url.pathname
console.log(currentPath)
---
<div class="max-w-2xl mx-auto flex justify-between items-center ">
	<a class={`${currentPath === '/' ? 'underline decoration-wavy' : ''}`}>hosenur.dev</a>
	<div class="flex gap-2.5">
		<a href="/blogs" class={`${currentPath === '/blogs' ? 'underline decoration-wavy' : ''}`}>blog</a>
		<a href="/projects" class={`${currentPath === '/projects' ? 'underline decoration-wavy' : ''}`}>projects</a>
		<a href="/gallery" class={`${currentPath === '/gallery' ? 'underline decoration-wavy' : ''}`}>gallery</a>
	</div>
</div>